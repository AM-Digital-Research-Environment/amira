// Re-export from the lib so the route module and the components stay in
// sync without one knowing about the other's path.
export { COMPARE_TYPES, type CompareType } from '$lib/components/compare/compareTypes';
