# Python dependencies for WissKI Dashboard data enrichment scripts
# (pins verified against PyPI latest at time of writing)
requests>=2.33.1
pymongo>=4.17.0

# Thumbnail fetcher (scripts/fetch_thumbnails.py): downloads each unique
# previewImage URL once, downsizes, and writes a WebP under static/thumbnails/.
# Pillow is the only new requirement; requests is already pinned above.
pillow>=12.2.0

# Knowledge-graph pipeline (scripts/generate_knowledge_graphs.py):
# networkx provides the graph data structure, Louvain community detection,
# and PageRank. scipy is optional but strongly recommended -- networkx uses
# it to speed up PageRank by an order of magnitude on mid-sized graphs.
networkx>=3.6.1
scipy>=1.17.1

# Semantic embeddings pipeline (scripts/generate_embeddings.py):
# Versions verified against PyPI (latest at time of writing). google-genai is
# the Gemini Python SDK; umap-learn projects the 768-dim embeddings down to
# 2D for the scatter viz; numpy is the numeric backbone (transitive for
# umap-learn, pinned here for clarity); python-dotenv loads the API key from
# the repo's .env file so the script doesn't depend on shell exports.
google-genai>=1.73.1
umap-learn>=0.5.12
numpy>=2.4.4
python-dotenv>=1.2.2

# Cluster publications fetcher (scripts/fetch_publications.py):
# bibtexparser parses the bulk BibTeX exports from ERef Bayreuth (projekt
# view) and EPub Bayreuth (Africa Multiple division view). v1.4.x is the
# stable line; v2 is still in beta at time of writing.
bibtexparser>=1.4.4,<2

# Cluster-website reconciliation (scripts/find_missing_publications.py):
# beautifulsoup4 parses the cluster's TYPO3-rendered "Recent Publications"
# page so we can diff its entries against the ERef division-340050 BibTeX
# export and emit a Zotero-importable list of references that aren't yet
# in ERef.
beautifulsoup4>=4.14.3
